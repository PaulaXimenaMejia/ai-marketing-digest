# [Your Name] Reports — Continuity Log

**Purpose:** Running memory for an automated digest. Each scheduled run reads
this doc before writing its summary, then appends a new entry — so insights
build on each other instead of resetting every time.

---

## Signal Tracker
*(living — overwritten each run, not appended)*

The small number of things currently worth watching. A metric graduates here
only after it moves meaningfully or repeats across 2+ periods. Things that
normalize get removed.

| Theme / Metric | First flagged | Direction | Status | Last updated |
|---|---|---|---|---|
| *(empty — populates after first run)* | | | | |

---

## Open Questions Carried Forward

Questions raised in a digest that haven't been answered yet. Each run checks
whether new data answers any of these before adding new ones.

- *(empty — populates after first run)*

---

## Log

Newest entries at the top. Each entry: date, sources pulled, signal summary,
questions raised, what got resolved from last period.

──────────────────────
*(entries appended below by each scheduled run)*
